#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char a[] = "Antoinette";
	char b[] = "Bismark";

	write("%s and %s are the authors of this repository\n", a, b);

	return (0);
}
